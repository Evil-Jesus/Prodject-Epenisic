using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Zentronic;

public class World : MonoBehaviour
{

    public static Grid3di navGrid = new Grid3di(32, 32, 0);

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
